name: 'Setup Go'
description: 'Set up Go — run actions/checkout before calling this'

runs:
  using: composite
  steps:
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.22'
